// import React from 'react';
// import { mount } from 'enzyme';
// import { expect } from 'chai';
// import sinon from 'sinon';

// import { Nav } from '../../../src/components/navbar';

// describe('Nav', () => {
//   const props = {
//     logout: sinon.spy(),
//     auth: { isLoggedIn: true, user: { firstName: 'Rowland' } }
//   };
//   const wrapper = mount(<Nav {...props} />);
//   it('should have a prop called auth', () => {
//     expect(wrapper.props.auth).to.be.defined; // eslint-disable-line
//   });
//   it('should have a logout action when a user clicks logout', () => {
//     expect(wrapper.props.logout).to.be.defined; // eslint-disable-line
//   });
// });
